version: '3'

services:
  frontend:
    build:
      context: ./front-end
    ports:
      - "3000:3000"
    volumes:
      - ./front-end:/app
      - /app/node_modules # tránh ghi đè node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev
